# Examples

This folder holds sanitized contribution packet examples.

Examples should show the full loop:

```text
triage -> repo load -> scoped implementation -> validation -> review loop -> evidence ledger
```

Do not include private tokens, private issue context, employer-confidential
details, or personal data.

Use `contribution-packet-template.md` to start a new example.

